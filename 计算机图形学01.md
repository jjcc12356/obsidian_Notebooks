计算机图形学(computer graphics)
“CG”是计算机图形学(ComputerGraphics)的缩写。
==利用计算机技术进行视觉设计和产生的领域通称为CG。==
计算机图形学是一门研究如何利用计算机表示、生成、处理和显示图形的学科。图形有两类：
- 基于线条表示的几何图形，如线框图、工程制图、等高线地图口基于材质、纹理和光照表示的真实感图形
基础：
- 线性代数、数据结构、程序设计语言(常用C/C++,Visual Studio)相关学科：
- 数学、模式识别、图像处理
==图形的两种表示方法：==
- 参数法：
	在设计阶段采用几何方法建立数学模型时，用形状参数和属性参数描述图形的方法。口形状参数可以是线段的起点、终点
	属性参数包括颜色、线型、宽度等非几何参数
- 点阵法：
	在显示阶段用具有颜色信息的像素点阵来表示图形的方法，描述的图形称为图像口→==计算机图形学就是研究将图形的表示法从参数法转换为点阵法的一门学科==
地图的定义
地图是根据一定的数学法则，将地球（或其他星球）上的自然和社会现象，通过制图综合所形成的信息，==运用符号系统缩绘到平面上的图形==，以传递它们的数量和质量，在时间和空间上的分布和发展变化。
计算机地图制图的概念
以计算机硬件设备为基础，在相应软件系统支持下，以数字格式对地图制图要素进行采集处理与管理，按照地图制作的规范进行符号化、图版制作与输出，并提供地图自动分析的全过程。
![[Pasted image 20260908150022.png]]
![[Pasted image 20260908150046.png]]
光栅扫描显示器(RSD)：画点设备
- 出现于20世纪70年代
- 电子束强度可以变化，易生成颜色连续变化的真实感图形点阵单元发生器，并可控制每个点阵单元的颜色，这些点阵单元称为像素不能直接画直线线，只能用靠近直线路径的像素点集来近似地表示只有在绘制45度、水平和垂直线段时像素点的位置才是准确的
- 其它情况下的直线段呈锯齿状，称为直线的走样
光栅扫描显示器(RSD)：位面与帧缓冲
- 帧缓冲器是显存内用于存储图像的连续内存空间
- 存储屏幕上每个像素的颜色信息，使用位面与屏幕像素一一对应
- 1位面：只使用一位(bit)，非0即1，屏幕只能显示黑白二色图像(黑白显示器)
光栅扫描显示器(RSD)：位面与帧缓冲
- 每个像素用一个字节(byte)表示，帧缓冲需要8个位面，8位数模转换器
- 可以表示2^8种灰度，称为灰度显示器
- 帧缓冲容量：1024x768x8=6 291 456 bit
![[Pasted image 20260908151014.png]]
在MFC框架中，我们画图通常是在视图类（比如 `CMyView`）的一个叫 `OnDraw` 的函数里进行。系统会传给你一个参数 `CDC* pDC`
- **类（Class）**：就像是“画家”的**职业说明书**，里面规定了画家能干嘛（能画点、画线）。`CDC` 就是MFC里自带的“设备上下文（画家）说明书”。
    
- **指针（Pointer，即 `*`）**：你可以把它理解为**画家的工牌号（内存地址）**。`CDC* pDC` 意思是，系统把一位名叫 `pDC` 的具体画家的工牌递给了你。
    
- **调用（`->` 箭头符）**：当你拿着工牌，想让这位画家干活时，C++里用 `->` 这个符号。比如 `pDC->SetPixel(...)`，通俗翻译就是：“呼叫工牌号为pDC的这位画家，去执行画点的动作”。
MFC框架采用了经典的“文档-视图”（Doc-View）架构。打个通俗的比方：

- **Doc文件（文档类）**：相当于“后厨仓库”，负责存放你的图形数据（比如多边形的顶点坐标都在这里）。
    
- **View文件（视图类）**：相当于“餐厅的透明展示橱窗”，负责把仓库里的数据“画”在屏幕上让用户看到。
我们开始绘制一点。
```
void CTestView::OnDraw(CDC* /*pDC*/)
{
    CTestDoc* pDoc = GetDocument();
    ASSERT_VALID(pDoc);
    if (!pDoc)
        return;

    // TODO: 在此处为本机数据添加绘制代码
}
```
系统自动生成时，括号里可能是 `CDC* /*pDC*/`，它把 `pDC` 这个画家的名字注释掉了。你只需要把注释符 `/*` 和 `*/` 删掉，变成 `CDC* pDC`，然后把我们的脚手架代码写在 `// TODO:` 的下面就可以了.
```
void CMyGraphicsView::OnDraw(CDC* pDC)
{
    // 1. 安全检查：如果系统没给我们派来画家（工牌是空的），就直接退出
    if (!pDC)
        return;

    //准备坐标变量

	int x = 100;

	int y = 100;

	//准备颜色变量，我们要纯红色，填写RGB(255,0,0)

	COLORREF myColor = RGB(255, 0, 0);

	//指挥画家画点

	pDC->SetPixel(x, y, myColor);
}
```
如果 `pDC` 是空的（相当于拿着空工牌对着空气说话），你强行调用 `pDC->SetPixel`，你的程序会**瞬间闪退崩溃**。在VS2022里，会弹出一个吓人的框写着“引发了异常: 读取访问冲突”。这是C++初学者最容易踩的坑，以后遇到程序毫无征兆地崩溃，第一反应就要怀疑是不是用了“空指针”。
从画点到画线：
画直线：
在屏幕上画线，本质上就是**连续画一串相邻的点**。

我们先复习一下初中数学。直线的方程是 $y = kx + b$。

其中，斜率 $k = \frac{\Delta y}{\Delta x}$。

斜率的物理意义是：**$x$ 每前进 1 个单位，$y$ 就会增加 $k$ 个单位。**

在计算机屏幕上，像素格子是离散的（必须是整数）。如果我们让 $x$ 每次加 1（也就是向右挪一个像素），那新的 $y$ 就等于旧的 $y$ 加上斜率 $k$。这就是图形学中最经典的 **DDA（数值微分分析法）画线算法**。
我们将画一条从 `(10, 10)` 到 `(110, 60)` 的蓝色直线。仔细阅读注释，根据刚才的数学推导，把空填上：
```
void CMyGraphicsView::OnDraw(CDC* pDC)
{
    if (!pDC) return;

    // 1. 定义起点和终点
    int x0 = 10, y0 = 10;
    int x1 = 110, y1 = 60;

    // 2. 计算差值和斜率
    // 注意：用 float 是为了保留小数，如果用 int，0.5 会被截断成 0
    float dx = x1 - x0;
    float dy = y1 - y0;
    float k = dy / dx;  // 斜率

    // 3. 准备追踪当前坐标
    float current_y = y0;

    // 4. 开始循环画点！
    // 循环逻辑：x 从起点 x0 一直增加到终点 x1
    for (int x = x0; x <= x1; x++) 
    {
        // 画当前点：因为像素坐标不能是小数，所以 (int)(current_y + 0.5) 是实现四舍五入
        pDC->SetPixel(x, (int)(current_y + 0.5), RGB(0, 0, 255));

        // 为下一个点做准备：x在for循环里已经自己 +1 了，那 current_y 应该怎么变化？
        current_y = /* 【空 E：请根据上面的数学推导，写出current_y的更新逻辑】 */;
    }
}
```
答案为：
```
//画直线
//定义起点和终点
int x0 = 10, y0 = 10;
int x1 = 110, y1 = 60;
//计算差值和斜率，用float保留小数
float dx = x1 - x0;
float dy = y1 - y0;
float k = dy / dx;
//准备追踪当前坐标
float current_y = y0;
//开始循环画点！
//循环逻辑：x从x0到终点x1
for (int x = x0; x <= x1; x++)
{
	//画当前点，因为像素坐标不能是小数，所以（int）(current_y+0.5)是实现四舍五入
	pDC->SetPixel(x, (int)(current_y + 0.5), RGB(0, 0, 255));
	//更新当前y坐标
	current_y = current_y + k;
```
![[Pasted image 20260908152539.png]]
我们现在的逻辑是每次让 $x$ 增加 1。假设我们现在要画一条非常陡峭的直线，比如起点是 `(10, 10)`，终点是 `(20, 100)`（$x$只走了10步，但$y$要走90步）。如果你还是用 `for (int x = ...; x++)` 这个让 $x$ 每次加 1 的框架去画，**你觉得画出来的线在视觉上会变成什么样？是一条连续的线，还是一串稀疏的虚线（点阵）？** 为什么？
当 $\Delta y > \Delta x$（即斜率绝对值大于1）时，如果还强行让 $x$ 每次加 1，$y$ 就会一次跨越好几个像素，导致画出来的线像虚线一样断开。
为了解决“陡峭直线变成虚线”的问题，我们需要对刚才的算法进行升级，让它变成能适应所有方向的**通用DDA算法**。
