# 任务一 影像文件的镶嵌
==**（多景栅格如何统一成一个连续空间数据集 核心包括空间参考、波段数量、像元类型、像元大小、重叠区域处理、NoData/黑边、镶嵌至新栅格）**==
- 查看影像头文件（或者属性）
- 提取栅格信息，包括栅格的行数，列数、波段数、空间分辨率、像元的最小可能值，最大可能值、影像坐标系的椭球长半径
- ==*空间分辨率*==：像元大小的单位要确定，如果是投影坐标系，单位是m。如果是地理坐标系，单位是度。估算公式为（40000÷360×像元大小）如4.5e-06≈0.5m（40000*0.0000045/360=0.0005km=0.5m）
- ==*像元的最小可能值和最大可能值*==，也即==像元的取值范围==：关注像素类型和像素深度，如无符号整型8位，取值范围就是0~255。要看是几位有无符号。
- ==*椭球体的椭圆长半径’*==：长半径a（Semimajor Axis）短半径b(Semiminor)
- ==*计算统计数据*==：右击属性可以计算
## 方法一 镶嵌到新栅格（符合提交要求的方案，其余方法可做辅助）
- 使用查找可以看像元值![[Pasted image 20260727163813.png]]
- **==镶嵌运算符==**：如果影像有黑边或者白边，不能使用Last，查看黑边的像元值可以选择Maximum或者Mean。
## 方法二 设置NoData
-  查看黑边或者白边的值
- 在元信息中设置每个band的==NoData==值（查看黑边或者白边的值）
## 方法三 符号系统中设置背景值
- 在图层属性中的符号系统里，打开==*显示背景值*==
# 任务二 创建点线面文件
==**空间参考：坐标系与什么一直，需确认，是不是地理坐标系？那么X和Y是经纬度，单位是degree，几何长度直接计算很可能得到度，几何面更不能理解为平方米，所以在length 、area、 distance、buffer这些立马想到投影**==
==**大批量按照属性自动融合以及人工少量修改合并**==
## 绘制带空洞的面要素
## 绘制具有相邻边的面要素
# 任务三 编辑要素

## 导入点
- 方法一：==XY转点==
- 方法二：==打开表文件，右击显示XY点==，再导出数据
## 合并点图层
- 按shift键多选点
- 或者打开属性表使用Shift选择连续的，使用Ctrl选择不连续的
- 右击copy
- 右击Paste，选择要粘贴的目标要素（前提是目标要素是可见的）
## 编辑线和面要素
- ==编辑折点==
- ==高级编辑==：**延长线**（选择两要素，先点击延长到的要素，后点击待延长要素）
- **裁剪**：先点击裁剪基准要素，再点击待裁剪要素(点击待裁剪的区域)
- **合并**：合并相同图层的多个要素
- **炸开**：![[Pasted image 20260727173002.png]]
可以发挥多部件至单部件的作用

==**基础影像构建：四景影像——元数据检查——影像镶嵌**==