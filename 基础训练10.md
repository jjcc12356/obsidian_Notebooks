三维分析工具条：创建等高线，检索最陡线，创建通视线（可设置偏移），
创建三维点，然后在图层右击图层转要素，三维线、三维面
使用观察点工具 属性表中Obs1 obs2 表示观察点，为0，表示不能被这个观察点看到，obs1=0 obs=0，则value为0 ，obs1=1，obs2=1，则value等于3
![[Pasted image 20260916091919.png]]
如果值为6，说明只能被第2和3观察点看到，如果值为7，只能被1、2、3观察点看到。
使用排列组合计算。假设五个观察点，则有C50+C51+C52+C53+C54+C55=24种。
短整型数据只占两个字节，整型为4个，2×8=16，所以只能不超过16.
可以使用属性选择工具，obs1=1，表示观察点1的可见的栅格
可以把属性表导出为Excel

怎么让其余4个观察点的范围最大
因为有的观察点有很大的范围是重叠的。
应该排除只能被某点观测到的范围，取观测范围最大的情况
