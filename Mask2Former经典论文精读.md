# 论文背景

2017-2021年，图像分割研究呈现明显碎片化趋势。语义分割、实例分割与全景分割各自形成独立技术路线，导致研究与落地难以统一推进。
## 语义分割：
语义分割
Semantic Segmentation
代表架构：DeepLab、SegFormer
- 对每个像素进行分类，
- 关注场景级理解。
- 能区分“车”与“路”
- 不区分同类个体。
![[Pasted image 20260919151347.png]]
## 实例分割：
代表架构：Mask R-CNN、SOLO
- 在识别类别的同时，区分不同目标实例。
- 适用于自动驾驶、质检等个体级任务。
![[Pasted image 20260919151445.png]]
## 全景分割
代表架构：Panoptic FPN、UPSNet
- 统一建模前景实例与背景类别。
- 追求更完整、精细的场景解析。
![[Pasted image 20260919151453.png]]
## 行业痛点：资源分散与高维护成本
不同任务往往依赖独立模型与部署流程，算法迁移困难、工程维护复杂，造成资源分散与成本升高。
# 作者动机——通用分割的诉求
![[Pasted image 20260919151645.png]]
论文目标：Mask2Former实现了“架构通用”与权重通用”，而更高阶的“训练流程通用」则由后续的OneFormer完成。
# 前序工作——MaskFormer
MaskFormer(NeurIPS 2021)提出关键问题：图像分割任务，能否被重新定义为“mask classification（掩码分类）？这一视角推动了分割范式转变。
## 传统范式：逐像素分类
![[Pasted image 20260919151930.png]]
- 对每个像素独立预测类别
- 输出H×W类别图
- 容易割裂目标整体性
局限在于离散、碎片化的
## 新范式：掩码分类
- 直接预测一组(Mask,Class)
- 对每个掩码对应完整目标/区域
- 更好保留目标整体性
优势：统一、整体建模
![[Pasted image 20260919151958.png]]
核心洞察：MaskFormer将分割问题从像素级分类，转化为基于掩码集合的统一预测问题。
# MaskFormer的痛点——为什么SOTA不到？
## 训练收敛慢
模型初始阶段，Query需要对全图进行无差别全局关注，难以快速定位有效目标区域。这种注意力的分散导致特征提取效率低，需要更多迭代步数才能稳定收敛，显著增加了训练的时间成本与算力消耗。
## 小目标效果差
解码器仅采用单一尺度的特征图进行推理，缺乏多尺度特征融合的能力。对于图像中像素占比极低的小目标，其关键语义信息极易在特征下采样过程中丢失，导致Query无法有效捕捉目标特征，分割精度大幅下降。
## 实例分割落后
在复杂场景的实例分割任务中，MaskFormer的核心指标明显落后于同期主流的MaskR-CNN系列算法。面对物体重叠、遮挡等复杂情况时，实例间特征易产生混淆，模型难以精准区分不同目标的边界与归属，限制了实际应用表现。
# MasksFormer
![[Pasted image 20260919152752.png]]
# 核心创新点1——Masked Attention
本质上是一种迭代修正机制——先全图搜索，再依据上一层Msak逐步聚焦目标。
![[Pasted image 20260919153208.png]]
![[Pasted image 20260919153220.png]]
就上Query更加将聚焦于前景
## Masked Attention数学形式
核心：在标准 attention logits 上加入由上一层 mask 产生的约束项M，使 query 只能聚合前景信息。
![[Pasted image 20260919153358.png]]
![[Pasted image 20260919153424.png]]

关键理解：M来自上一层的mask 预测，因此Masked Attention 本质上是一个迭代修正机制——用更好的mask 计算更好的attention。
# 核心创新点2——多尺度Deformable解码器
核心：用FPN多尺度特征+Deformable Attention，让解码器既看得细、又算得省。
![[Pasted image 20260919153702.png]]
重点：多尺度是检测/分割中的核心思想；Mask2Former 将FPN与 Deformable Attention 结合，让解码器既能处理小目标，又能控制计算成本。
# 解码器层结构
核心：每层解码器都执行3步一一query先彼此交互，再与多尺度特征交互，最后经过FFN；层末更新mask，作为下一层的约束M。
![[Pasted image 20260919154005.png]]
![[Pasted image 20260919154127.png]]
# 整体架构
![[Pasted image 20260919154224.png]]
## Backbone 骨干网络
采用 Swin Transformer 或 ResNet作为特征提取基础，从输入图像中高效提取多尺度、多分辨率的深层视觉特征，为后续处理提供高质量的语义与纹理信息。
## Pixel Decoder解码器
基于FPN特征金字塔结构，对Backbone输出的特征进行跨层级融合与上采样。有效聚合局部细节与全局语义，生成更具表达力的特征图，提升小目标感知能力。
## Transformer Decoder
架构核心逻辑单元，包含N个可学习的Query序列。经过9层深度迭代计算，每层执行Masked Attention机制，动态建模像素间的长距离依赖关系，精准定位目标轮廓。
## 任务最终输出
解码器最后一层直接输出N个(Mask,Class)二元组。每个结果独立对应场景中的一个物体实例，包含精确的掩码区域与类别预测，无需额外的后处理步骤即可完成分割。
# 实验结果一——全景分割SOTA
![[Pasted image 20260919154618.png]]
![[Pasted image 20260919154637.png]]
更少的训练次数
# 消融试验
![[Pasted image 20260919154707.png]]
![[Pasted image 20260919154715.png]]
![[Pasted image 20260919154724.png]]
![[Pasted image 20260919154730.png]]
![[Pasted image 20260919154737.png]]
给我的启示，看论文的时候不要只盯着对比实验，更要看消融试验，确定哪些模块的