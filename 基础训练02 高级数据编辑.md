## 重点：点线面之间的转化
### 点转线：点集转线
- 点要素要具有两个字段，一个是类别字段（如道路名）一个是顺序字段。
- 生成的线**不具备原有的点要素的属性**，需要通过连接保存，或者通过导出数据保存
### 点转面：要素转面（先进行点集转线生成闭合线）
- 新生成的面文件不具备原来点的属性，可以通过【**空间连接**】工具，使用**边界相接触**。
### 面转点：要素折点转点转点
### 测地线面积不支持计算
- 给数据框加个投影，不改变原始数据的坐标
- 或者对数据进行投影
- 或者使用【**添加几何属性**】工具，指定投影
## 三维展示
### 三维展示前要必须进行投影，否则单位不是米
==**FID不能默认为固定测量顺序、只修改数据框坐标系不代表数据完成投影、应避免在地理坐标系下直接计算面积、周长、高度、  **==
==**测量数据重构 TXT坐标——测量点——顺序连线——建筑面**==
==**地块质量控制与更新：Topology——重叠检查——几何修复——新规划边界**==
==**属性与几何信息完善：Excel Join——面积/周长——顶点坐标——ASCII**==
==**建筑三维重建：属性筛选——高度关联——投影统一——Extrusion——Multipatch**==