# 1、数据信息汇总

| 数据名称 | 行列数 (列 × 行) | 波段数 | 像元大小 (X/Y) | 像素深度 | NoData 值 | 空间坐标系 (WKID) | 原始体积 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| RGB 正射影像 | 41646 × 45809 | 4 | ~0.0242 米 | 8位无符号 | 0, 0, 0, 0 | WGS 84 UTM 49N (投影, 32649) | 7.11 GB |
| RGB 工程 DSM | 20823 × 22905 | 1 | ~0.0484 米 | 32位浮点型 | -9999 | WGS 84 UTM 49N (投影, 32649) | 1.78 GB |
| 多光谱 (Green) | 24026 × 26524 | 1 | ~4.19E-07 度 | 32位浮点型 | nan | WGS 1984 (地理, 4326) | 2.37 GB |
| 多光谱 (Red) | 24026 × 26524 | 1 | ~4.19E-07 度 | 32位浮点型 | nan | WGS 1984 (地理, 4326) | 2.37 GB |
| 多光谱 (NIR) | 24026 × 26524 | 1 | ~4.19E-07 度 | 32位浮点型 | nan | WGS 1984 (地理, 4326) | 2.37 GB |
| 多光谱 (RedEdge) | 24026 × 26524 | 1 | ~4.19E-07 度 | 32位浮点型 | nan | WGS 1984 (地理, 4326) | 2.37 GB |
| 多光谱工程 DSM | 20392 × 22513 | 1 | ~4.93E-07 度 | 32位浮点型 | -9999 | WGS 1984 (地理, 4326) | 1.71 GB |

# 2、预处理流程
## 投影栅格（重点在于求投影是像元大小）

	坐标系统一：多光谱工程中的都不是投影，需要进行投影栅格（设置像元大小）
	
	像元大小不能用度，需要计算
	
	考虑到图像的边界坐标：
	
	上 28.349790 deg
	
	下 28.339958 deg
	
	左 113.306175 deg
	
	右 113.316241 deg

**第一步：计算经纬度跨度（度）**

纬度跨度（南北方向，Y轴）：

$${28.349790}^{\circ} - {28.339958}^{\circ} = {0.009832}^{\circ}$$

经度跨度（东西方向，X轴）：

$${113.316241}^{\circ} - {113.306175}^{\circ} = {0.010066}^{\circ}$$

**第二步：将"度"转换为"米"**

地球是一个近似椭球体，赤道上的 1 度大约是 111.32
公里。但请注意，经度线的实际物理距离会随着纬度的升高而缩短（向两极收敛）。试验区所在的纬度大约是${28.34}^{\circ}$（湖南地区），这里必须引入一个余弦定理来进行修正：

Y轴物理高度（纬度 1 度约合 111,320 米，基本不随纬度剧变）：

$$0.009832 \times 111,320 \approx 1094.5\text{米}$$

X轴物理宽度（经度 1
度在${28.34}^{\circ}$处的实际距离为$111,320 \times cos\left( {28.34}^{\circ} \right) \approx 97,975\text{米}$）：

$$0.010066 \times 97,975 \approx 986.2\text{米}$$

**第三步：计算实际像元大小**

结合我们之前统计的该图层行列数（列数/X：24026，行数/Y：26524），我们把总长度平摊到每一个像素盒子上：

X方向像元大小：

$$986.2\text{米/}24026\text{像素≈}0.0410\text{米}$$

Y方向像元大小：

$$1094.5\text{米/}26524\text{像素≈}0.0412\text{米}$$

X 和 Y 方向的像元大小在物理世界中几乎是相等的正方形，均为 0.041 米（即
4.1 厘米）。

在 ArcGIS Pro 的 "投影栅格 (Project Raster)" 工具中，在 X/Y 像元大小
(Cell Size) 这一栏里，手动输入 0.041，然后完成投影转换。

将NiR和redege栅格投影之后再进行栅格计算器计算得到NDRE:

公式：$$NDRE = \frac{NIR - RedEdge}{NIR + RedEdge}$$
## 剥离DTM得到nDSM（重点在于焦点统计时设置的滑窗大小）
为了从DSM中剥离nDSM：

我们之前尝试过使用布料模拟算法进行剥离，但结果显示并不理想。

于是，我们可以通过焦点统计得到近似的DTM，然后相减得到。

其中，焦点统计中滑窗得大小设置不能直接随便设置
根据测量工具，我大概测了20个树冠的最大直径，取最大值为4.02m左右，走一遍公式推演：

**第一步：转换为基本像素**

$$4.02 \text{ 米} \div 0.0484 \text{ 米/像元} \approx 83.05 \text{ 个像元}$$

这意味着，研究区里最大的一棵油茶树，在 `RGB 工程 DSM` 这张图上大约占据了 83×83 的像素方阵。

**第二步：加上冗余系数（安全边界）**

为了保证移动窗口一定能“跨过”这棵最大的树，摸到旁边的裸露地面，我们乘上 1.2 的安全系数：

$$83 \times 1.2 \approx 99.6 \text{ 个像元}$$

**第三步：确定最终软件输入值**

在图像处理算法中，滑动窗口（Kernel）我们通常严格使用**奇数**，因为奇数矩阵有一个绝对的“中心像素”，这样在计算时不会发生哪怕半个像素的空间偏移。

离 99.6 最近的奇数是 101。
## 自动提取最小矢量边界（重点对数据进行统一空间范围）
**第一步：生成二值化掩膜 (Binarization)** 我们需要把 nDSM 中有数值的地方全变成 1，把 NoData 的地方保持为空。

- 打开 **“条件函数 (Con)”** 工具。
    
- **输入条件栅格**：选择你的 `nDSM` 图层。
    
- **表达式 (SQL)**：输入 `"Value" >= 0` （假设你的树高和地面都是大于等于0的，如果有极小负误差，可以写 `> -10`）。
    
- **输入条件为 true 时**：输入常量 `1`。
    
- **输入条件为 false 时**：留空（这样不符合条件的区域就会变成 NoData）。
    
- 运行后，你会得到一张只有 1 和空值的纯色栅格图。
    

**第二步：栅格转面 (Raster to Polygon)** 把刚才的纯色栅格直接转化为矢量多边形。

- 打开 **“栅格转面 (Raster to Polygon)”** 工具。
    
- **输入栅格**：刚才生成的二值化掩膜图。
    
- **字段**：保持默认的 `Value` 即可。
    
- **简化多边形 (Simplify polygons)**：**建议取消勾选**。这样生成的矢量边界会严格贴合栅格像素的锯齿状边缘，保证后期裁剪时像素级对齐。
    
**第三步：内缓冲**，因为生成的边界面不一定真的能覆盖所有栅格范围，为保险起见，做一个-0.5m的内缓冲。

**第四步：终极裁剪 (Clip & Mask)** 现在你手头拥有了一个完美的、代表 nDSM 有效范围的矢量多边形（Polygon）。

- 再次祭出 **“按掩膜提取 (Extract by Mask)”** 工具。
    
- 把 `RGB正射影像` 和 `NDRE`，统统用这个新生成的矢量多边形裁剪一遍。
# 代码重构执行方案
### 前端方案进化路线与后续规划


- **方案 1（已完成）：** 5 通道直接拼接进入 Swin Backbone。
    import torch  
import torch.nn as nn  
from transformers import Mask2FormerForUniversalSegmentation, Mask2FormerConfig  
  
# 1. 加载默认模型  
config = Mask2FormerConfig()  
model = Mask2FormerForUniversalSegmentation(config)  
  
# 2. 定位到原来的第一层（3通道）  
old_conv = model.model.pixel_level_module.encoder.embeddings.patch_embeddings.projection  
  
# 3. 实例化一个新的卷积层（5通道）  
new_conv = nn.Conv2d(  
    in_channels=5,  # 重点：扩展为 RGB(3) + nDSM(1) + NDRE(1)    out_channels=old_conv.out_channels,  
    kernel_size=old_conv.kernel_size,  
    stride=old_conv.stride,  
    padding=old_conv.padding,  
    bias=(old_conv.bias is not None)  
)  
  
# 4. 核心：权重外科手术 (Weight Surgery)with torch.no_grad():  
    # A. 把预训练好的 RGB 权重原封不动地复制给前 3 个通道  
    new_conv.weight[:, :3, :, :] = old_conv.weight.clone()  
  
    # B. 对新增的 2 个通道（nDSM, NDRE）的权重进行初始化  
    # 这里我们采用 Kaiming 正态分布初始化（常用于深度学习网络）  
    torch.nn.init.kaiming_normal_(new_conv.weight[:, 3:, :, :])  
  
    # C. 如果有偏置项（Bias），一并复制  
    if old_conv.bias is not None:  
        new_conv.bias.copy_(old_conv.bias)  
  
# 5. 移花接木，把做好的新层塞回模型里  
model.model.pixel_level_module.encoder.embeddings.patch_embeddings.projection = new_conv  
# #6. 伪造一组 5 通道无人机切片数据：Batch Size = 2, Channels = 5, Height = 512, Width = 512  
dummy_inputs = torch.randn(2, 5, 512, 512)  
  
## 7. 运行前向传播  
outputs = model(pixel_values=dummy_inputs)  
  
print("前端重构成功！新的第一层结构为：")  
print(model.model.pixel_level_module.encoder.embeddings.patch_embeddings.projection)  
  
print("\n--- 前向传播测试成功！ ---")  
print("分类预测 Logits 维度:", outputs.class_queries_logits.shape)  
print("掩膜预测 Logits 维度:", outputs.masks_queries_logits.shape)
- **方案 2（待构建）：** 在 5 通道输入后、进入 Backbone 前，插入一个 **CBAM 注意力模块**。让模型自己学习“哪一个通道（RGB 还是高程 nDSM/植被 NDRE）对找油茶树更重要”。
    
- **方案 3（待构建）：** 设计**多分支架构**，RGB 走原版 Swin 分支，nDSM 和 NDRE 走独立的轻量级 CNN/Swin 分支，然后在特征图（Feature Map）层级进行融合（Mid-Fusion）。
    
- **方案 4（待构建）：** 方案 3 的多分支架构 + 在融合位置加入 CBAM 注意力。